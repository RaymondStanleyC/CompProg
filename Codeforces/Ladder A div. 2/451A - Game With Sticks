// 451A - Game With Sticks

#include <iostream>
#include <vector>

using namespace std;

int main(){

	int h, v;
	cin >> h >> v;
	if((h%2 == 0 && v%2==0) || ((h>v && h%2!=0 && v%2==0) || (v>h && v%2!=0 && h%2==0))){
		cout << "Malvika" << endl;
	}else{
		cout << "Akshat" << endl;
	}
	return 0;
}

